# circuits-rl

Reinforcement learning through the circuits substrate.

The frontier thesis: a value function is a continuation, Bellman backup is
composition in `Prob`, and value iteration is a fold. This package wires that
claim into concrete oracles on a small gridworld.

See `coffee/loom/frontier.md` for the roadmap and `app/axioma.hs` for the
verification executable.
